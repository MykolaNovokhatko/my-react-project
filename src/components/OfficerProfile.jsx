export default function OfficerProfile(name, specialization, age) {
    return <div>
        <p>name: {name}</p>
        <p>Age: {age}</p>
        <p>specialization: {specialization} </p>
    </div>;
}